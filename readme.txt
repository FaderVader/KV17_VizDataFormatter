A small script to assist handling location data in Viz Artist. Used for KV17.

Allows user to edit up to 4 sets of coordinates, including name.
The save-button will produce both an internal file, and a file suitable for copy-paste into a viz-script.

GUI is WPF-based